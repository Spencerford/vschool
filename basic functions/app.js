function add(num, num2) {
    return (num + num2);
}
add(1, 2);


function sub (x, y) {
    return (x - y);
}

sub(10, - 8);

function mult (numb, cool) {
    return (numb * cool);
}

mult (4, 4);

function sub2 (num1, num3) {
     return (num1 - num3);
}

var bin = sub2 (-4, 6);

console.log(sub2(-4, 6));